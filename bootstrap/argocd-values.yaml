global:
  nodeSelector:
    kubernetes.io/hostname: master512

server:
  service:
    type: ClusterIP

dex:
  enabled: false

notifications:
  enabled: false

server:
  ingress:
    enabled: true
    hostname: argocd.gurkeschale.ch
    ingressClassName: traefik-public
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-cloudflare
    tls: true
